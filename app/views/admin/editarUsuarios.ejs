<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="../../public/css/style.css">
</head>

<body>
    <h1>Editar usuario</h1>


    <% if (erros){%>
        <ul>
            <% for (let i=0; i <erros.length;i++) { %>
                <li>
                    <%= erros[i].msg %>
                </li>
                <% }%>
        </ul>
        <%}%>
                <form action="/admin/salvarUsuario" method="post">
                    <label>Nome</label>
                    <br>
                    <input type="text" name="nome" value="<%=usuario[0].nome%>">
                    <br>
                    <label>Email</label>
                    <br>
                    <input type="email" name="email" value="<%=usuario[0].email%>">
                    <br>
                    <label>Funcao</label>
                    <br>
                    <select name="id_tipo_usuario">
                        <% for(let i=0;i < tipos.length;i++){%>
                            <%if(tipos[i].id_tipo_usuario == usuario[0].id_tipo_usuario) {%>
                                <option value="<%= tipos[i].id_tipo_usuario %>" selected>
                                    <%= tipos[i].descricao%>
                                </option>
                            <% }
                                else{
                            %>
                                <option value="<%= tipos[i].id_tipo_usuario %>">
                                    <%= tipos[i].descricao%>
                                </option>
                        <%    }
                        }%>
                    </select>
                    <br>
                    <br>
                    <input type="hidden" value="<%= usuario[0].id_usuario%>" name="id">
                    <input type="submit" value="enviar">
                </form>
</body>

</html>